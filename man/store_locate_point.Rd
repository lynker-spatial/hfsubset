% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/locate.R
\name{store_locate_point}
\alias{store_locate_point}
\title{Locate the divide containing a point}
\usage{
store_locate_point(store, xy, crs_pt = 4326, layer = "divides", ...)
}
\arguments{
\item{store}{A store object.}

\item{xy}{Numeric length-2 vector, \code{c(x, y)} (e.g. \code{c(lon, lat)}).}

\item{crs_pt}{CRS of \code{xy}. Default EPSG:4326 (WGS84 lon/lat).}

\item{layer}{Polygon layer to test containment against. Default "divides".}
}
\value{
An \code{sf} of the matching divide row(s); zero rows if the point falls
outside the fabric.
}
\description{
Resolve an \code{xy} coordinate to the divide (catchment) polygon that contains
it. Used by \code{\link[=hfsubset]{hfsubset()}} to support point-based origins ("subset above where
I clicked"). Mirrors the lat/lon lookup in CIROH's NGIAB_data_preprocess:
transform the point to the layer CRS, hit the spatial index for candidates,
then test exact containment.
}
\keyword{internal}
