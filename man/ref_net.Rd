% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network.R
\docType{data}
\name{ref_net}
\alias{ref_net}
\title{Flowpath References Table}
\format{
A tibble with 2,860,009 rows and 4 variables:
\describe{
\item{flowpath_id}{\code{integer}. Unique identifier for a flowpath.}
\item{vpuid}{\code{factor}. Vector Processing Unit (VPU) identifier indicating
the regional partition of the hydrologic fabric.}
\item{flowpath_toid}{\code{integer}. Identifier of the downstream flowpath.
\code{NA} if terminal.}
\item{hl_reference}{\code{factor}. Optional external reference code(s)
associated with the flowpath, e.g. NBI bridge IDs, watershed outlets,
or terminus identifiers. May be \code{NA}.}
}
}
\source{
Derived from lynker-spatial data products.
}
\usage{
ref_net
}
\description{
A tibble mapping flowpaths to upstream/downstream connectivity and external
reference identifiers. Each row represents a relationship between a
\code{flowpath_id} and either its downstream feature or an associated
hydrologic reference.
}
\details{
This dataset is intended to support graph operations and external
lookups in hydrologic network analysis. A single \code{flowpath_id} can
appear multiple times when multiple \code{hl_reference} entries are
associated with it. Use dplyr::distinct where it makes sense :)
}
\examples{
data(ref_net)
dplyr::filter(ref_net, flowpath_id == 931090014)
}
\keyword{datasets}
