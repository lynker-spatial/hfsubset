% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/optimize.R
\name{optimize_gpkg}
\alias{optimize_gpkg}
\title{Index and optimize a hydrofabric GeoPackage}
\usage{
optimize_gpkg(gpkg, extra_cols = character(), verbose = FALSE)
}
\arguments{
\item{gpkg}{Path to a GeoPackage written by \code{\link[=hfsubset]{hfsubset()}}.}

\item{extra_cols}{Additional column names to index when present.}

\item{verbose}{Logical; report indices created and R-trees rebuilt.}
}
\value{
The \code{gpkg} path, invisibly.
}
\description{
Make a written subset cheap to query and re-subset. Three things, borrowed
from the \code{verify_indices()} / R-tree handling in CIROH's
NGIAB_data_preprocess:
}
\details{
\enumerate{
\item \strong{Attribute indices} on the id / foreign-key columns of every table
(e.g. \code{flowpath_id}, \code{divide_id}, \code{vpuid}). \code{sf} writes the spatial
R-tree but leaves attribute tables (\code{network}, \verb{*-attributes}) and
non-geometry id columns unindexed, so \verb{WHERE flowpath_id IN (...)}
re-subsetting scans the whole table without these.
\item \strong{Spatial R-tree verification.} GDAL builds \verb{rtree_<layer>_<geom>}
at write time (\code{SPATIAL_INDEX=YES}). We confirm each feature layer has
one and rebuild any that is missing, so the index is present \emph{and}
freshly built (optimal) before anything copies it downstream.
\item \strong{\code{ANALYZE} + \verb{PRAGMA optimize}} so SQLite's planner actually uses
the new indices and the R-tree statistics are current.
}
}
