% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/traverse.R
\name{store_upstream_ids}
\alias{store_upstream_ids}
\title{Resolve the upstream id table for an origin flowpath}
\usage{
store_upstream_ids(store, origin_fp_id, vpu, has_vpuid, cache = TRUE, ...)
}
\arguments{
\item{store}{A store object.}

\item{origin_fp_id}{Origin \code{flowpath_id}.}

\item{vpu}{Origin VPU id (character) or \code{NA}.}

\item{has_vpuid}{Logical; whether the network carries a \code{vpuid} column.}

\item{cache}{Logical; reuse the per-(source, VPU) graph (default path).}
}
\value{
A data.frame of upstream id columns.
}
\description{
Return the subset of the network's id table (one row per upstream
flowline/flowpath) that drains to \code{origin_fp_id}. The result carries
whichever of \code{flowpath_id}, \code{flowpath_toid}, \code{poi_id}, \code{divide_id},
\code{flowline_id} the schema provides — enough to filter every downstream layer.
}
\details{
Dispatch picks the cheapest correct traversal for the backend:
\itemize{
\item \code{ogr_store} pushes the walk into SQLite with a recursive CTE, touching
only upstream rows (no whole-VPU materialization, no graph build).
\item the default collects the VPU's edges once and walks an in-memory igraph,
caching both per (source, VPU) for batch reuse.
}
}
\keyword{internal}
